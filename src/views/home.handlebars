<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <title>Productos</title>
</head>
<body>

    <h1>¡Bienvenido, {{userName}}!</h1> <!-- Esto debería mostrar el primer nombre -->


    <form action="/api/sessions/logout" method="POST">
        <button type="submit">Logout</button>
    </form>

    <h2>Mis Productos</h2>

    <div class="contenedorProductos">
        {{#each productos}}
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{this.title}}</h5>
                <p class="card-text">{{this.description}}</p>
                <p> ${{this.price}} </p>
                <a href="#" class="btn btn-primary">Agregar al Carrito</a>
            </div>
        </div>
        {{/each}}
    </div>

    <br>

    <div>
        {{#if hasPrevPage}}
        <button class="btn btn-outline-success" type="button">
            <a href="/products?page={{prevPage}}" style="color: green; text-decoration: none;">Anterior</a>
        </button>
        {{/if}}

        {{#if hasNextPage}}
        <button class="btn btn-outline-info" type="button">
            <a href="/products?page={{nextPage}}" style="color: rgb(0, 255, 238); text-decoration: none;">Siguiente</a>
        </button>
        {{/if}}

        <p> Pagina {{currentPage}} de {{totalPages}} </p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>